<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.school.management.infrastructure.persistence.asset.AssetCategoryMapper">

    <resultMap id="CategoryResultMap" type="com.school.management.infrastructure.persistence.asset.AssetCategoryPO">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="category_code" property="categoryCode"/>
        <result column="category_name" property="categoryName"/>
        <result column="category_type" property="categoryType"/>
        <result column="depreciation_years" property="depreciationYears"/>
        <result column="unit" property="unit"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="remark" property="remark"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
        <result column="asset_count" property="assetCount"/>
    </resultMap>

    <select id="selectAllWithAssetCount" resultMap="CategoryResultMap">
        SELECT c.*,
               COALESCE(a.asset_count, 0) AS asset_count
        FROM asset_category c
        LEFT JOIN (
            SELECT category_id, COUNT(*) AS asset_count
            FROM asset
            WHERE deleted = 0
            GROUP BY category_id
        ) a ON c.id = a.category_id
        WHERE c.deleted = 0
        ORDER BY c.sort_order, c.id
    </select>

</mapper>
